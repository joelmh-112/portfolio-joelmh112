---
interface Props {
  className?: string;
}
const { className } = Astro.props;
---

<span class={`bg-primary bg-opacity-20 text-primary font-semibold px-3 py-1 rounded-full text-sm ${className ?? ""}`}>
  <slot />
</span>

